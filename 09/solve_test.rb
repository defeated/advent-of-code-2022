require_relative "../test_helper"

describe "day 09" do
  before do
    data = File.readlines(__dir__ + "/data.txt", chomp: true)
  end

  it "..." do
    expect(0).must_equal 1
  end

  it "..." do
    skip
    expect(0).must_equal 1
  end
end
